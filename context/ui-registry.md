# UI Registry

Living document. Update after every component is built. Check here before building a new component — match existing patterns exactly before inventing new ones.

---

## How to Use

1. Before building any component, check if a similar one already exists here.
2. If yes — match its exact classes.
3. If no — build it following ui-rules.md and ui-tokens.md, then add it here.

---

## Components

_Empty. Add components here as they are built, e.g.:_

```
### LoadStatusBadge
components/loads/LoadStatusBadge.tsx
Classes: rounded-full px-2.5 py-0.5 text-xs font-medium, color map per ui-tokens.md
Props: status: LoadStatus
```
